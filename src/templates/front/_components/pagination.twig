{% set aligment = aligment ?? 'start' %} {# start, center or end #}
{% set size = size ?? false %} {# sm or lg #}

<ul class="pagination justify-content-{{ aligment }}{% if size %} pagination-{{ size }}{% endif %}">
    {% block pagination_items %}
    {% endblock %}
</ul>