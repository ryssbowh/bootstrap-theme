{% extends "blocks/block" %}

{% block content %}
    <a href="{{ currentSite.getBaseUrl }}" class="h3 text-white">
        <img width="50" src="{{ theme_url('icon.svg') }}"> {{ currentSite.name }}
    </a>
{% endblock %}