{% extends "fields/_field" %}

{% block content %}
    <{{ options.tag }} class="text-center">
        {% if options.linked and element.url %}
            <a href="{{ element.url }}">{{ value }}</a>
        {% else %}
            {{ value }}
        {% endif %}
    </{{ options.tag }}>
{% endblock %}